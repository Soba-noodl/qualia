/** Central registry of Gemini model identifiers used across Edge Functions. */

/** Primary multimodal model — used for analysis, synth, and most AI calls. */
export const GEMINI_MODEL = "gemini-3-flash-preview";

/** Lightweight flash model — used for summarisation and low-stakes inference. */
export const GEMINI_FLASH_MODEL = "gemini-2.5-flash";
